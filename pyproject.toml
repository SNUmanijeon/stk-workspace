[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "stk-workspace-toolkit"
version = "0.1.0"
description = "Portable workspace structure and setup utilities for STK/Python studies"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
