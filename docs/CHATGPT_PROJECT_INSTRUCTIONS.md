# Suggested ChatGPT Work project instructions

Use the accessible STK workspace manual at `docs/STK_WORKFLOW.md` and consult `docs/CATALOG.md` before changing an analysis. Use `templates/README.md` and the template registry for reusable artifact sources. Consult local `scenarios/WORKSPACE_MAP.md` for active studies.

Reuse validated code and components. Keep local scenarios and generated artifacts under `scenarios/<name>`. Record assumptions, units, reference frames, code/input identity, template versions, and actual validation evidence. Do not treat unpopulated asset/template directories as implemented methods.

If the workspace files are unavailable, say which source is missing and request access to the relevant material. A cloud ChatGPT Project does not gain access to a local directory merely because its path appears here.
